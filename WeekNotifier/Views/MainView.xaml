<Window x:Class="WeekNotifier.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Main" Height="450" Width="800"
        DataContext="{Binding MainViewModel, Source={StaticResource Locator}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Canvas Grid.Column="0" Height="50" Width="50">
            <Image Width="50" Source="{Binding CalendarImage}"/>
            <Canvas Top="3">
                <TextBlock Width="50" Text="{Binding WeekNumber}" TextAlignment="Center" FontSize="36" FontWeight="Bold"/>
            </Canvas>
        </Canvas>

        <Image Grid.Column="1" Width="50" Source="{Binding DrawnImage}"/>
    </Grid>

</Window>